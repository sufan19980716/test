less的使用
  less是一个css预处理语言,扩展了css语法(变量,函数,混合(mixin)),使css更易维护

  使用方式有两种:
    1. 在nodejs中运行(编译),生成一个对应css文件

    2. 在浏览器中使用
      在HTML中直接引入less文件,通过less.js去编译,动态生成对应css文件(一般不用)

      事先写好less文件,借助工具生成对应的css文件,在HTML中直接引入该css文件




